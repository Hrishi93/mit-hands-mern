const add = (n1, n2) => (n1+n2);
const sub = (n1, n2) => (n1-n2);
const mul = (n1, n2) => (n1*n2);
const div = (n1, n2) => (n1/n2);

export {add, sub, mul, div};

/*
abstarct Parent{
    abstract show(); // we need to override that abstract method in Child Class
}
*/